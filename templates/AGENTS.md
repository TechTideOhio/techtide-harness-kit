# AGENTS.md

## Purpose
- Store starter asset templates for contributors.

## Rules
- Keep templates minimal and schema-compatible.
- Update templates when required metadata fields change.
- Do not put provider-specific guidance in generic templates.
- Run `npm run validate` after template changes if catalog examples are affected.

